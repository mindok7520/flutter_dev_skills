# 자료 저장소 구조

이 저장소의 구조는 개발 자료와 대상 프로젝트용 템플릿을 분리한다. Flutter 앱 코드는 포함하지 않는다.

| 위치 | 책임 |
| --- | --- |
| `AGENTS.md` | 공통 규칙·문서 지도·자료/앱 작업 구분 |
| `docs/` | 분야별 정책·작업 절차·검증 방법의 원본 |
| `prompts/` | 사용자가 작업할 때 선택하는 62개 프롬프트 |
| `.agents/skills/` | 반복 수행하는 전문 작업 절차 29개 |
| `.github/`, `.cursor/`, 루트 어댑터 | 공통 지침을 도구별로 연결 |
| `templates/project/` | 대상 제품의 정의·아키텍처 작성 양식 |
| `templates/flutter/` | 선택적으로 통합하는 설정·개발 도구·CI |
| `scripts/` | 충돌을 검사하는 설치·자료 검증 도구 |
| `config/` | 최초 요구 파일·후속 추가 파일과 현재 프롬프트/스킬의 검증 목록 |
| `tests/` | 자료 도구의 경계·실패·충돌 검증 |

"Canonical source"는 같은 규칙의 기준이 되는 원본이다. 정책 원본은 docs이며 어댑터에는 전체 정책을 복제하지 않는다. 문서 변경 시 관련 링크와 프롬프트를 검사한다.

## 복사 경계

설치 도구는 공통 자료를 대상에 복사하고 PROJECT·ARCHITECTURE는 `templates/project/`의 양식을 사용한다. 자료 저장소 전용 CI·초기화 기록·라이선스·검증 코드는 대상 앱에 넣지 않는다. Flutter 설정 샘플은 자동 덮어쓰지 않고 사용자가 기존 파일에 통합한다.

## 대상 앱 설계의 기본안

[앱 아키텍처 지침](docs/architecture/APP_ARCHITECTURE.md)은 기능별 구성과 UI·상태·데이터 경계의 권장안이다. "MVVM"은 화면과 화면 상태·동작을 분리하는 방식이다. 실제 상태관리·라우팅·저장소는 대상 앱의 규모와 기존 구조에 맞춰 결정한다. 문서의 `lib/` 경로와 예시 명령은 대상 프로젝트를 가리킨다.

자료 설치는 앱 생성·의존성 설치·Git 변경·실제 배포를 수행하지 않는다. 외부 작업 권한과 제품 결정은 대상 프로젝트의 이슈에서 다룬다.
